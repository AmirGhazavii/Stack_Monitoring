groups:
- name: blackbox.rules
  rules:
  - alert: BlackboxTargetDown
    expr: probe_success == 0
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: "Target {{ $labels.instance }} is down"
      description: "The blackbox-exporter probe failed for target {{ $labels.instance }}." 
